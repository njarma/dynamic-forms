import { DynamicFormInputDirective } from './dynamic-form-input.directive';

describe('DynamicFormInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicFormInputDirective();
    expect(directive).toBeTruthy();
  });
});
